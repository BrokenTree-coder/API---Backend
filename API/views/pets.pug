doctype html
html(lang="pt-BR")
  head
    title= title
  body
    h1 Lista de Pets Cadastrados

    //- Bloco condicional: só mostra a lista se o array 'pets' não estiver vazio [cite: 476]
    if pets.length > 0
      //- Laço de repetição: cria um card para cada item no array 'pets' [cite: 477]
      each pet in pets
        div.pet-card(style="border: 1px solid #ccc; padding: 10px; margin-bottom: 10px; border-radius: 5px;")
          //- Renderiza o valor da propriedade 'nome' do pet atual [cite: 479]
          h3= pet.nome
          p
            strong Espécie: 
            span= pet.especie
          p
            strong Raça: 
            span= pet.raca
    else
      p Nenhum pet cadastrado no momento.
